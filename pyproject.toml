[project]
name = "dash-vibe"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.33",
    "httpx>=0.27",
    "pandas>=2.2",
    "folium>=0.16",
    "streamlit-folium>=0.20",
    "pydantic>=2.7",
]

[tool.uv]
dev-dependencies = []
